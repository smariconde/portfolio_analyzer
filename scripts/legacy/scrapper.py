"""Deprecated script kept for backwards compatibility."""

from pathlib import Path

if __name__ == "__main__":
    print("`scrapper.py` is deprecated. Use `scrapper_diario.py` or `portfolio-analyzer run commodities`.")
    print(f"Archived source: {Path('scripts/legacy_archive/scrapper.py').resolve()}")
